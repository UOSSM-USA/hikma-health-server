// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";

function shapeErr(op, msg) {
  return {
    TAG: "InvalidShape",
    operator: op,
    message: msg
  };
}

function parseAll(depth, arr) {
  let out = [];
  let err;
  let i = 0;
  while (err === undefined && i < arr.length) {
    let r = parse(depth + 1 | 0, arr[i]);
    if (r.TAG === "Ok") {
      out.push(r._0);
    } else {
      err = r._0;
    }
    i = i + 1 | 0;
  };
  let e = err;
  if (e !== undefined) {
    return {
      TAG: "Error",
      _0: e
    };
  } else {
    return {
      TAG: "Ok",
      _0: out
    };
  }
}

function parseArray(depth, op, raw) {
  if (Array.isArray(raw)) {
    return parseAll(depth, raw);
  } else {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidShape",
        operator: op,
        message: "expects an array of arguments"
      }
    };
  }
}

function parseFixed1(depth, _op, raw, build) {
  return Stdlib_Result.map(parse(depth + 1 | 0, raw), r => build(r));
}

function parseFixed2(depth, op, raw, build) {
  let e = parseArray(depth, op, raw);
  if (e.TAG !== "Ok") {
    return {
      TAG: "Error",
      _0: e._0
    };
  }
  let args = e._0;
  if (args.length >= 2) {
    return {
      TAG: "Ok",
      _0: build(args[0], args[1])
    };
  } else {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidShape",
        operator: op,
        message: "expects at least 2 arguments"
      }
    };
  }
}

function parseFixed3(depth, op, raw, build) {
  let e = parseArray(depth, op, raw);
  if (e.TAG !== "Ok") {
    return {
      TAG: "Error",
      _0: e._0
    };
  }
  let args = e._0;
  if (args.length >= 3) {
    return {
      TAG: "Ok",
      _0: build(args[0], args[1], args[2])
    };
  } else {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidShape",
        operator: op,
        message: "expects at least 3 arguments"
      }
    };
  }
}

function parseStatic(depth, op, raw, build) {
  return Stdlib_Result.map(parseArray(depth, op, raw), args => build(args));
}

function parseStaticMin(depth, op, raw, min, build) {
  let e = parseArray(depth, op, raw);
  if (e.TAG !== "Ok") {
    return {
      TAG: "Error",
      _0: e._0
    };
  }
  let args = e._0;
  if (args.length >= min) {
    return {
      TAG: "Ok",
      _0: build(args)
    };
  } else {
    return {
      TAG: "Error",
      _0: {
        TAG: "InvalidShape",
        operator: op,
        message: `expects at least ` + min.toString() + ` arguments`
      }
    };
  }
}

function parseStaticPermissive(depth, raw, build) {
  if (Array.isArray(raw)) {
    return Stdlib_Result.map(parseAll(depth, raw), args => build(args));
  }
  return Stdlib_Result.map(parse(depth + 1 | 0, raw), r => build([r]));
}

function parseUnaryUnwrap(depth, raw, build) {
  if (Array.isArray(raw) && raw.length === 1) {
    return Stdlib_Result.map(parse(depth + 1 | 0, raw[0]), r => build(r));
  }
  return Stdlib_Result.map(parse(depth + 1 | 0, raw), r => build(r));
}

function parseOp(depth, op, raw) {
  switch (op) {
    case "!" :
      return parseUnaryUnwrap(depth, raw, r => ({
        TAG: "Not",
        _0: r
      }));
    case "!!" :
      return parseUnaryUnwrap(depth, raw, r => ({
        TAG: "NotNot",
        _0: r
      }));
    case "!=" :
      return parseStaticMin(depth, op, raw, 2, args => ({
        TAG: "Neq",
        _0: args
      }));
    case "!==" :
      return parseStaticMin(depth, op, raw, 2, args => ({
        TAG: "StrictNeq",
        _0: args
      }));
    case "%" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Mod",
        _0: r
      }));
    case "*" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Mul",
        _0: r
      }));
    case "+" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Add",
        _0: r
      }));
    case "-" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Sub",
        _0: r
      }));
    case "/" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Div",
        _0: r
      }));
    case "<" :
      return parseStaticMin(depth, op, raw, 2, args => ({
        TAG: "Lt",
        _0: args
      }));
    case "<=" :
      return parseStaticMin(depth, op, raw, 2, args => ({
        TAG: "Lte",
        _0: args
      }));
    case "==" :
      return parseStaticMin(depth, op, raw, 2, args => ({
        TAG: "Eq",
        _0: args
      }));
    case "===" :
      return parseStaticMin(depth, op, raw, 2, args => ({
        TAG: "StrictEq",
        _0: args
      }));
    case ">" :
      return parseStaticMin(depth, op, raw, 2, args => ({
        TAG: "Gt",
        _0: args
      }));
    case ">=" :
      return parseStaticMin(depth, op, raw, 2, args => ({
        TAG: "Gte",
        _0: args
      }));
    case "??" :
      return parseStaticPermissive(depth, raw, args => ({
        TAG: "Coalesce",
        _0: args
      }));
    case "all" :
      return parseFixed2(depth, op, raw, (c, b) => ({
        TAG: "All",
        _0: c,
        _1: b
      }));
    case "and" :
      return parseStatic(depth, op, raw, args => ({
        TAG: "And",
        _0: args
      }));
    case "cat" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Cat",
        _0: r
      }));
    case "exists" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Exists",
        _0: r
      }));
    case "filter" :
      return parseFixed2(depth, op, raw, (c, b) => ({
        TAG: "Filter",
        _0: c,
        _1: b
      }));
    case "?:" :
    case "if" :
      break;
    case "in" :
      return parseFixed2(depth, op, raw, (n, h) => ({
        TAG: "In",
        _0: n,
        _1: h
      }));
    case "length" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Length",
        _0: r
      }));
    case "map" :
      return parseFixed2(depth, op, raw, (c, b) => ({
        TAG: "Map",
        _0: c,
        _1: b
      }));
    case "max" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Max",
        _0: r
      }));
    case "merge" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Merge",
        _0: r
      }));
    case "min" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Min",
        _0: r
      }));
    case "missing" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Missing",
        _0: r
      }));
    case "missing_some" :
      return parseFixed2(depth, op, raw, (a, b) => ({
        TAG: "MissingSome",
        _0: a,
        _1: b
      }));
    case "none" :
      return parseFixed2(depth, op, raw, (c, b) => ({
        TAG: "None_",
        _0: c,
        _1: b
      }));
    case "or" :
      return parseStatic(depth, op, raw, args => ({
        TAG: "Or",
        _0: args
      }));
    case "preserve" :
      return {
        TAG: "Ok",
        _0: {
          TAG: "Preserve",
          _0: raw
        }
      };
    case "reduce" :
      return parseFixed3(depth, op, raw, (c, b, i) => ({
        TAG: "Reduce",
        _0: c,
        _1: b,
        _2: i
      }));
    case "some" :
      return parseFixed2(depth, op, raw, (c, b) => ({
        TAG: "Some_",
        _0: c,
        _1: b
      }));
    case "substr" :
      return parseStaticMin(depth, op, raw, 2, args => ({
        TAG: "Substr",
        _0: args
      }));
    case "throw" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Throw",
        _0: r
      }));
    case "try" :
      return parseStaticPermissive(depth, raw, args => ({
        TAG: "Try",
        _0: args
      }));
    case "val" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Val",
        _0: r
      }));
    case "var" :
      return parseFixed1(depth, op, raw, r => ({
        TAG: "Var",
        _0: r
      }));
    default:
      return {
        TAG: "Error",
        _0: {
          TAG: "UnknownOperator",
          _0: op
        }
      };
  }
  return parseStatic(depth, op, raw, args => ({
    TAG: "If",
    _0: args
  }));
}

function parse(depthOpt, json) {
  let depth = depthOpt !== undefined ? depthOpt : 0;
  if (depth > 256) {
    return {
      TAG: "Error",
      _0: "MaxDepthExceeded"
    };
  }
  if (Array.isArray(json)) {
    return Stdlib_Result.map(parseAll(depth, json), elems => ({
      TAG: "ArrayOf",
      _0: elems
    }));
  }
  if (json === null) {
    return {
      TAG: "Ok",
      _0: {
        TAG: "Literal",
        _0: json
      }
    };
  }
  switch (typeof json) {
    case "object" :
      let entries = Object.entries(json);
      let match = entries.length;
      if (match === 0) {
        return {
          TAG: "Ok",
          _0: {
            TAG: "Literal",
            _0: json
          }
        };
      }
      if (match !== 1) {
        return {
          TAG: "Error",
          _0: {
            TAG: "MultiKeyObject",
            _0: entries.map(param => param[0])
          }
        };
      }
      let match$1 = entries[0];
      return parseOp(depth, match$1[0], match$1[1]);
    default:
      return {
        TAG: "Ok",
        _0: {
          TAG: "Literal",
          _0: json
        }
      };
  }
}

let maxDepth = 256;

export {
  maxDepth,
  shapeErr,
  parseAll,
  parseArray,
  parseFixed1,
  parseFixed2,
  parseFixed3,
  parseStatic,
  parseStaticMin,
  parseStaticPermissive,
  parseUnaryUnwrap,
  parseOp,
  parse,
}
/* No side effect */
